# 待确认事项

以下内容尚未最终决定。进入对应实现前，应更新本文件和相关专题文档。

## 产品与部署

- 本地版是否同时交付 Private Connected 和 Air-Gapped，还是第一期只交付 Air-Gapped。
- 支持哪些 Kubernetes 发行版：K3s、RKE2、标准 Kubernetes、OpenShift 等。
- 完全断网客户使用哪种通知渠道，是否接受只使用内部事件控制台。
- 离线授权和离线升级的产品策略。
- 第一批客户环境的节点、服务、日志量和保留周期。

## 监控与数据

- 第一期默认日志后端最终选择 Loki 还是 OpenSearch。
- Trace 的默认保留周期和采样策略。
- 是否需要长期保存所有原始告警，还是只保存事件引用。
- PostgreSQL、对象存储和遥测后端的备份恢复目标。
- 虚拟机和传统设备第一期优先支持的协议与中间件列表。

## 模型

- 本地模型候选及其企业许可证。
- 第一版目标是 7B、14B 还是更大的量化模型。
- Embedding 和 Reranker 模型候选。
- 中文运维评测集、工具调用评测和根因候选命中率标准。
- Private Connected 是否允许把 S2 脱敏证据发送到云端模型。

## Agent 与执行

- `08-local-mcp-server.md` 中的第一期 MCP 工具清单和 Schema 最终确认。
- Agent Runtime 是否在 Mastra 技术验证通过后正式采用 Mastra。
- 第一期 TypeScript/Node.js 版本、包管理器和 Monorepo 工具。
- 单 Agent Workflow 何时升级为 Supervisor/多 Agent 架构。
- L2 诊断动作的审批方式。
- 哪些动作可以被认定为低风险自动执行。
- 审批超时、多人审批、撤销和紧急停止机制。
- 自动修复的验证、回滚和失败升级规则。

## 安全

- 客户身份源：本地账号、LDAP、OIDC、AD 或其他方式。
- 租户、项目、环境和角色的权限模型。
- PII、凭据、内部 IP、代码和漏洞信息的分级规则。
- 审计日志保留周期和防篡改要求。
- 安全风险识别是否只消费已有扫描结果，何时扩展主动扫描。
- Air-Gapped 环境的本地身份认证与 MCP HTTP Token 签发方式。

## 商业与运维

- 是否面向单一企业部署，还是未来支持多租户平台。
- 产品自身的健康监控和故障自诊断方式。
- 模型、规则、知识库和漏洞库的离线升级频率。
- 支持服务、版本兼容矩阵和生命周期策略。
